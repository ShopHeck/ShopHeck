// Placeholder until we run `npx supabase gen types typescript`.
// This compiles fine but provides no row-level type safety yet.
// Regenerate with:
//   npx supabase gen types typescript --project-id=tdkihhzlwobtdwhycbcx --schema=public > src/lib/database.types.ts
export type Database = unknown;
